IF NOT EXISTS(SELECT NAME FROM SYSOBJECTS WHERE NAME='KCEQUI' AND XTYPE='U')
BEGIN
   CREATE TABLE DBO.KCEQUI (
   IDEQUIPO VARCHAR(20) NOT NULL,
   DESCRIPCION VARCHAR(255),
   ESTADO VARCHAR(10),
   PRUEBA1 VARCHAR(10),
   PRUEBA2 VARCHAR(10),
   RESULTADO1 VARCHAR(10),
   RESULTADO2 VARCHAR(10),
   ESTERIL BIT,
   CNSPRUEBA VARCHAR(20),
   F_VENCE DATETIME)
   
   ALTER TABLE KCEQUI
   ADD CONSTRAINT KCEQUIIDEQUIPO
   PRIMARY KEY CLUSTERED (IDEQUIPO)
END
IF NOT EXISTS ( SELECT O.object_id, C.column_id, C.NAME FROM  SYS.OBJECTS O INNER JOIN SYS.columns C ON O.object_id = C.object_id  WHERE O.NAME = 'KCEPRU'  AND   C.NAME = 'IDPRUEBA')
BEGIN
   ALTER TABLE KCEPRU  ADD IDPRUEBA VARCHAR(20)
END
