IF NOT EXISTS(SELECT NAME FROM SYSOBJECTS WHERE NAME='TURNOV' AND XTYPE='U')
BEGIN
   CREATE TABLE TURNOV(
   IDTURNOV    INT IDENTITY(1,1),
   PERIODO      VARCHAR(7),
   USUARIO      VARCHAR(20),
   TIPOPERSONAL VARCHAR(20),
   TIPO_USUARIO VARCHAR(20),
   CODNOV       VARCHAR(20),
   HNOV         INT,
   ESTADO       VARCHAR(10),
   CNSPAGO      VARCHAR(20)
   )
   ALTER TABLE TURNOV
   ADD CONSTRAINT TURNOVIDTURNOV
   PRIMARY KEY CLUSTERED (IDTURNOV)
END
IF NOT EXISTS(SELECT * FROM SYS.indexes WHERE name='TURNOVPERIODOUSUARIOCODNOV')
BEGIN
   CREATE UNIQUE INDEX TURNOVPERIODOUSUARIOCODNOV ON TURNOV (PERIODO,USUARIO,CODNOV)
END