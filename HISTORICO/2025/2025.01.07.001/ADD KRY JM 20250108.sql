IF NOT EXISTS(SELECT NAME FROM SYSOBJECTS WHERE NAME='PPTCRO' AND XTYPE='U')
BEGIN
   CREATE TABLE PPTCRO (
   IDPPTCRO INT IDENTITY(1,1),
   IDTERCERO VARCHAR(20) NOT NULL,
   IDPLAN VARCHAR(6) NOT NULL,
   ANIO VARCHAR(4),
   MES  SMALLINT,
   VALOR DECIMAL(14,2),
   IDSERVICIO VARCHAR(20)
   )
   ALTER TABLE PPTCRO
   ADD CONSTRAINT PPTCROIDPPTCRO
   PRIMARY KEY CLUSTERED (IDPPTCRO)
END
IF NOT EXISTS(SELECT * FROM SYS.indexes WHERE NAME='PPTCROIDTERIDPLANANIOMES')
BEGIN
   CREATE UNIQUE INDEX PPTCROIDTERIDPLANANIOMES ON PPTCRO (IDTERCERO,IDPLAN,ANIO,MES)
END
